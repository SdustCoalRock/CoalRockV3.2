unit RockClass;
{$WARN SYMBOL_PLATFORM OFF}

interface

uses
  ActiveX, Mtsobj, Mtx, ComObj, RockPressure_TLB, StdVcl,SysUtils,Windows,Messages,math,
  ComServ,StrUtils ;

type
  TPressureClassTPressureClass = class(TMtsAutoObject, IC_PressureClass)

 // protected
  private

  protected

  public


  end;
 var
    P_Stope:TPressureClassTPressureClass;
implementation


//---------变量定义


initialization
  TAutoObjectFactory.Create(ComServer, TPressureClass, Class_PressureClass,
    ciMultiInstance, tmApartment);
end.
